export interface TenseInterface {
  name: string;
  html?: string;
  strategies: Array<Array<number>>;
  pronounts: Array<Array<Object>>;
  auxiliaries: Array<Array<Object>>;
  verbs: Array<Array<Object>>;
}
